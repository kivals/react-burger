export enum iconTypes {
    CURRENCY= 'currency',
    BURGER = 'burger',
    LIST = 'list',
    PROFILE = 'profile',
    DRAG = 'drag',
    CLOSE = 'close'
}

export enum iconColorTypes {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    ERROR = 'error'
}